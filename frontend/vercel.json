{
  "version": 2,
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist/front/browser",
  "installCommand": "cd frontend && npm install",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}